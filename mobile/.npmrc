# React Native / Expo hanno spesso peer-deps disallineate tra react e react-native.
# Come da prassi RN, accettiamo la risoluzione legacy per avere install riproducibili
# (vale anche per `npx expo install`).
legacy-peer-deps=true
